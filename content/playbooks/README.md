# Channel Playbooks

Per-channel handling standards. Each channel has its own SLA, script variations, and quality expectations.

- `lsa.md` — Google Local Services Ads. Callback SLA: {{price:lsa_callback_sla}}.
- `web.md` — Web form submissions. Callback SLA: {{price:web_lead_callback_sla}}.
- `posh.md` — After-hours virtual receptionist handoff.
- `referral.md` — Customer referrals — how to acknowledge, credit, and follow up with the referrer.

Playbooks reference the master CSM SOP (`sop.csm.*`) rather than restating shared standards. They contain only the channel-specific overrides.
